# การตั้งค่าระบบ / System Settings

**คู่มือนี้เหมาะสำหรับ**: Admin ที่ต้องการเข้าใจและจัดการการตั้งค่าระบบ

---

## ภาพรวม / Overview

การตั้งค่าระบบกำหนด**พฤติกรรมพื้นฐาน**ของระบบการจองนัดหมาย

**หมายเหตุ**: **การตั้งค่าส่วนใหญ่เป็น Hardcoded** ในเวอร์ชันปัจจุบัน (ไม่มี UI สำหรับแก้ไข)

**ฟีเจอร์ในอนาคต**: หน้า Settings สำหรับ Admin แก้ไขค่าได้

---

## 1. เวลาทำการ / Working Hours

### ค่าปัจจุบัน / Current Settings

**วันทำการ**: จันทร์ - ศุกร์
**เวลาทำการ**: 08:30 - 16:30 (8.5 ชั่วโมง)
**พักกลางวัน**: 12:00 - 13:00 (1 ชั่วโมง)

**Hardcoded ใน**: `/src/lib/constants.ts` หรือ `/src/lib/timeSlots.ts`

```typescript
const WORKING_HOURS = {
  start: "08:30",
  end: "16:30",
  lunchStart: "12:00",
  lunchEnd: "13:00",
  workingDays: [1, 2, 3, 4, 5], // Mon-Fri
};
```

---

### การเปลี่ยนแปลง / Changing Working Hours

**ปัจจุบัน**: ต้องแก้ไข Source Code

**อนาคต**: หน้า Settings → Working Hours
- เลือกวันทำการ (Mon-Sun checkboxes)
- ตั้งเวลาเริ่ม/สิ้นสุด
- ตั้งเวลาพักกลางวัน

---

## 2. ระยะเวลาช่อง / Slot Duration

### ค่าปัจจุบัน / Current Settings

**Slot Duration**: **30 นาที**

**ความหมาย**: นัดหมายแต่ละช่องมีระยะเวลา 30 นาที

**ตัวอย่าง**:
```
08:30-09:00 (ช่องที่ 1)
09:00-09:30 (ช่องที่ 2)
09:30-10:00 (ช่องที่ 3)
...
```

**Hardcoded ใน**: `/src/lib/constants.ts`

```typescript
const SLOT_DURATION_MINUTES = 30;
```

---

### การเปลี่ยนแปลง / Changing Slot Duration

**ปัจจุบัน**: ต้องแก้ไข Source Code

**อนาคต**: หน้า Settings → Slot Duration
- เลือก 15, 30, 45, 60 นาที

**ผลกระทบ**:
- ถ้าเปลี่ยนเป็น 15 นาที → ช่องเพิ่มขึ้น (มากขึ้น 2 เท่า)
- ถ้าเปลี่ยนเป็น 60 นาที → ช่องลดลง (ลดลงครึ่งหนึ่ง)

---

## 3. บริการ / Services

### บริการที่มี / Available Services

| Service | Duration | Description (TH) | Description (EN) |
|---------|----------|------------------|------------------|
| HARDWARE | 30 min | ซ่อมฮาร์ดแวร์ | Hardware repairs |
| SOFTWARE | 30 min | ติดตั้งซอฟต์แวร์ | Software installation |
| NETWORK | 45 min | แก้ไขเครือข่าย | Network troubleshooting |
| ACCOUNT | 15 min | จัดการบัญชี | Account management |

**กำหนดใน**: `/prisma/schema.prisma`

```prisma
enum Service {
  HARDWARE
  SOFTWARE
  NETWORK
  ACCOUNT
}
```

---

### การเปลี่ยนแปลง / Changing Services

**ปัจจุบัน**: ต้องแก้ไข Database Schema

**ขั้นตอน**:
1. แก้ไข `/prisma/schema.prisma`
2. เพิ่ม/ลบ Service ใน enum
3. `npx prisma migrate dev`
4. อัปเดต Frontend (เพิ่มตัวเลือกใน Dropdown)

**อนาคต**: หน้า Settings → Services
- เพิ่ม/ลบบริการ
- แก้ไขชื่อ, ระยะเวลา, คำอธิบาย

---

## 4. สถานะนัดหมาย / Appointment Statuses

### สถานะที่มี / Available Statuses

| Status | Description (TH) | Description (EN) | Color |
|--------|------------------|------------------|-------|
| PENDING | รอยืนยัน | Awaiting confirmation | 🟡 Yellow |
| CONFIRMED | ยืนยันแล้ว | Confirmed | 🔵 Blue |
| COMPLETED | เสร็จสิ้น | Completed | 🟢 Green |
| CANCELLED | ยกเลิก | Cancelled | 🔴 Red |

**กำหนดใน**: `/prisma/schema.prisma`

```prisma
enum Status {
  PENDING
  CONFIRMED
  COMPLETED
  CANCELLED
}
```

---

### การเปลี่ยนแปลง / Changing Statuses

**ไม่แนะนำ**: สถานะเป็นส่วนสำคัญของ Business Logic

**ถ้าจำเป็น**:
1. แก้ไข `/prisma/schema.prisma`
2. `npx prisma migrate dev`
3. อัปเดต Frontend (Badge colors, filters)

---

## 5. วันหยุด / Holidays

### ปัจจุบัน / Current Implementation

**วันหยุด**: ไม่มี (ยังไม่รองรับ)

**พฤติกรรม**:
- เสาร์-อาทิตย์ ถูกบล็อกอัตโนมัติ (Hardcoded)
- วันหยุดนักขัตฤกษ์ยังไม่ถูกบล็อก

---

### การเพิ่มวันหยุด / Adding Holidays (Future Feature)

**อนาคต**: หน้า Settings → Holidays
- เพิ่มวันหยุด (date picker)
- ตั้งชื่อวันหยุด (เช่น "วันสงกรานต์")
- บล็อกการจองในวันนั้นอัตโนมัติ

**ตัวอย่าง**:
```
Date: 2026-04-13
Name: วันสงกรานต์
→ ลูกค้าไม่สามารถจองวันที่ 13 เม.ย. 2026
```

---

## 6. ข้อจำกัดการจอง / Booking Limits

### ค่าปัจจุบัน / Current Settings

**การจองล่วงหน้า**: **2 สัปดาห์** (14 วัน)
**จำนวนนัดหมายต่อลูกค้า**: **ไม่จำกัด**

**Hardcoded ใน**: `/src/app/api/slots/route.ts` หรือ Frontend validation

---

### การเปลี่ยนแปลง / Changing Limits

**อนาคต**: หน้า Settings → Booking Limits
- Maximum advance booking: 7, 14, 30 วัน
- Maximum appointments per customer: 1, 2, 3, 5, ไม่จำกัด

---

## 7. การแจ้งเตือน / Notifications

### การตั้งค่าปัจจุบัน / Current Settings

**Server-Sent Events (SSE)**: ✅ เปิดใช้งาน
**Toast Duration**: **5 วินาที**
**Reconnect Interval**: **5 วินาที** (ถ้าขาดการเชื่อมต่อ)

**Hardcoded ใน**: `/src/hooks/useNotifications.ts`

---

### การเปลี่ยนแปลง / Changing Settings

**อนาคต**: หน้า Settings → Notifications
- Toast duration: 3, 5, 10 วินาที
- Enable/Disable sound
- Enable/Disable browser notifications

---

## 8. ข้อมูลองค์กร / Organization Information

### ข้อมูลปัจจุบัน / Current Information

**ชื่อ**: Prachinburi Computer Center Help Desk
**ที่อยู่**: (ยังไม่ระบุ)
**เบอร์โทร**: (ยังไม่ระบุ)
**อีเมล**: (ยังไม่ระบุ)

**Hardcoded ใน**: Frontend components, README

---

### การเปลี่ยนแปลง / Changing Information

**อนาคต**: หน้า Settings → Organization
- Organization name
- Address
- Phone
- Email
- Logo upload

**จะปรากฏที่**:
- หน้า Dashboard (header)
- PDF reports
- อีเมลแจ้งเตือน (future)

---

## Best Practices / แนวทางที่ดี

### ✅ ทดสอบก่อนเปลี่ยนแปลง

- แก้ไขในสภาพแวดล้อม Development ก่อน
- ทดสอบการทำงาน
- Deploy ไป Production

---

### ✅ สำรองข้อมูลก่อนเปลี่ยน Schema

- ก่อนรัน `prisma migrate`
- สำรองฐานข้อมูล
- เผื่อต้อง rollback

---

### ✅ แจ้งทีมก่อนเปลี่ยนเวลาทำการ

- ถ้าเปลี่ยนเวลาทำการ
- แจ้งทีมและลูกค้าล่วงหน้า
- อัปเดตเว็บไซต์

---

## Troubleshooting / การแก้ปัญหา

**ปัญหา**: เปลี่ยนเวลาทำการแล้วแต่ไม่อัปเดต

**วิธีแก้**:
1. ล้าง Cache (Ctrl+Shift+R)
2. รีสตาร์ทเซิร์ฟเวอร์ (Dev: `npm run dev`)
3. เช็คว่าแก้ไขถูกไฟล์หรือไม่

---

**ปัญหา**: เพิ่ม Service ใหม่แต่ไม่แสดงใน Dropdown

**วิธีแก้**:
1. เช็คว่ารัน `prisma migrate` แล้วหรือไม่
2. อัปเดต Frontend (เพิ่มตัวเลือกใน Dropdown)
3. รีเฟรชหน้าจอ

---

**⬅️ ก่อนหน้า**: [48-Audit-History.md](48-Audit-History.md) | **➡️ ถัดไป**: [50-Admin-FAQs.md](50-Admin-FAQs.md)
